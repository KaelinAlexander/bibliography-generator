<ul>
<% @bibliography.citations.each do |citation| %>
<li><% if authors?(citation.text.authors) %><%= mla_cmos_authors(citation.text.authors) %><% end %><i><%= mla_cmos_title(citation.text.title) %></i>. <% if editors?(citation.text.authors) %> <%= mla_cmos_editors(citation.text.authors) %><% end %></li>
<% end %>
</ul>